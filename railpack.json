{
  "$schema": "https://railpack.io/schema/railpack.json",
  "phases": {
    "extensions": {
      "cmds": ["install-php-extensions gd zip pdo_mysql bcmath ctype curl dom fileinfo filter hash mbstring openssl pcre pdo session tokenizer xml"]
    },
    "build": {
      "cmds": [
        "npm run build",
        "mkdir -p storage/framework/{sessions,views,cache,testing} storage/logs bootstrap/cache && chmod -R a+rw storage",
        "php artisan optimize:clear",
        "php artisan config:cache"
      ]
    }
  }
}